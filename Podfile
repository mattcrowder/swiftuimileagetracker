# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'swiftuimileagetracker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftDate', '~> 6.0'
  pod 'PromisesSwift', '~> 1.2.8'
  # Pods for swiftuimileagetracker

  target 'swiftuimileagetrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'swiftuimileagetrackerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
